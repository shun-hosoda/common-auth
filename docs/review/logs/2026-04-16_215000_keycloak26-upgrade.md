# Review Log — Keycloak 26 アップグレード

## メタデータ
- 日時: 2026-04-16
- 対象: Keycloak 24 → 26 アップグレード
- レビュアー: Review Board（5人合議制）
- ラウンド: 1

## レビュー対象

| ファイル | 変更概要 |
|----------|---------|
| `auth-stack/docker-compose.yml` | KC 24.0→26.0、version削除、hostname v2、healthcheck→9000、KC_HEALTH_ENABLED追加、KC_BOOTSTRAP_ADMIN_USERNAME移行 |
| `auth-stack/keycloak/realm-export.json` | `basic`スコープ追加（oidc-sub-mapper）、全クライアントにbasicスコープ追加、defaultRoles→defaultRole移行 |
| `infra/docker-compose.yml` | KC 24.0.5→26.0、hostname v1オプション削除、KC_BOOTSTRAP_ADMIN_USERNAME移行、KC_HEALTH_ENABLED追加 |
| `infra/docker-compose-totp.yml` | KC 24.0.5→26.0、hostname v1オプション削除、KC_BOOTSTRAP_ADMIN_USERNAME移行、KC_HEALTH_ENABLED追加 |

## Phase 1〜3

### 判定: APPROVE（修正後）

### MUST FIX（修正済み）
1. ✅ `infra/docker-compose.yml` と `infra/docker-compose-totp.yml` の `KEYCLOAK_ADMIN` → `KC_BOOTSTRAP_ADMIN_USERNAME` に更新
2. ✅ `infra/` 両ファイルに `KC_HEALTH_ENABLED: "true"` 追加

### SHOULD FIX（修正済み）
3. ✅ `realm-export.json` の `defaultRoles` を KC 26の `defaultRole` 形式に移行

### CONSIDER（次回以降）
4. `infra/` のrealm-exportにも `basic` スコープ追加を検討（現状 infra/ はカスタムSPI依存の別フロー）
5. `KC_HOSTNAME` のURL形式（`http://localhost:8080`）はKC 26の開発環境では機能している

### GOOD
- `basic` スコープ＋`oidc-sub-mapper` の追加でKC 26のsub欠落を正確に解消
- healthcheckのポート 8080→9000 変更はKC 26マネジメントインターフェース分離に正確に対応
- `version: '3.8'` 削除（docker compose v2非推奨対応）

## テスト結果

| テスト | 結果 |
|--------|------|
| KC 26起動・Realmインポート | PASS (26.0.8) |
| defaultRole形式インポート | PASS |
| 全4ユーザーログイン | PASS (sub/tenant_id/roles 正常) |
| FastAPI /api/protected | PASS |
| React vitest 15テスト | 15/15 PASS |
| frontend-sdk jest 34テスト | 34/34 PASS |
| backend-sdk pytest 102テスト | 99 passed / 3 failed（KC26起因でない既存の問題） |
