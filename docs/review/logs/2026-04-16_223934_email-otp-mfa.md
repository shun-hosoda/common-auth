# Review Log — auth-stack Email OTP MFA導入

## メタデータ
- 日時: 2026-04-16 22:39:34
- 対象: Email OTP SPI導入（auth-stack）
- レビュアー: Review Board（5人合議制）
- ラウンド: /review

## レビュー対象
- 差分サマリー
  - `auth-stack/docker-compose.yml`: Keycloak providers マウント追加
  - `auth-stack/keycloak/realm-export.json`: MFAフローをTOTP/Email分岐へ再構成、`mfa-email-condition`追加、`email-authenticator-setup` Required Action追加、Email MFAテストユーザー追加
  - `auth-stack/keycloak/providers/download-providers.ps1`: Email OTPプロバイダーJAR取得スクリプト追加
  - `auth-stack/keycloak/providers/README.md`: providers運用手順追加
- 変更ファイル数: 4

## 実行チェック
- `git diff --cached`: ステージ済み差分なし（未ステージ差分をレビュー対象化）
- `python -m json.tool auth-stack/keycloak/realm-export.json`: OK
- `docker compose config` (in `auth-stack/`): OK
- 型チェックコマンド（`npx tsc --noEmit ...`）: ルート実行では `tsc` 未導入のため実行不可
  - 補足: 今回の変更対象に `.ts/.tsx/.py` の構造変更なし

## Phase 1 — 初見ラウンド

PM: Email OTPを選択可能にする要件に対し、フロー分岐とテストユーザー追加で検証導線まで整っており、ユーザー価値に直結している。

Arch: `mfa-gate` を条件 + サブフロー分岐に分離した設計は拡張しやすく、将来のMFA方式追加（例: WebAuthn）にも対応しやすい。

Eng: JARをGit管理せずスクリプト配布にした判断は妥当。`docker-compose.yml` も最小変更で既存構成を壊していない。

Sec: 外部JAR取得にチェックサム検証がなく、サプライチェーン面の防御が弱い点は改善余地。

DB: DBスキーマ変更がなく、tenant属性ベース運用との整合は維持されている。データ整合性リスクは低い。

## Phase 2 — 議論（クロスレビュー）
- Arch → Sec: サプライチェーン対策は同意。`download-providers.ps1` にSHA256固定値検証を加えると運用強度が上がる。
- Eng → PM: テストユーザーをrealm-exportに追加したため再現性は高いが、本番向けには初期ユーザー投入方針を分離した方がよい。
- Sec → Eng/PM: 現状は開発用スタックとして許容可能。ただし「本番利用禁止」または「検証手順必須」をREADMEに明記したい。
- DB → 全体: 既存のグループ属性デフォルト（totp）を温存しているため既存テナントへの副作用は限定的。

## Phase 3 — 判定

判定: APPROVE

[MUST FIX]
1. なし

[SHOULD FIX]
1. `auth-stack/keycloak/providers/download-providers.ps1` にJARのSHA256検証を追加し、改ざん検知を有効化する。
2. `auth-stack/keycloak/providers/README.md` に「開発用途前提」「本番適用時は署名/チェックサム検証必須」を追記する。

[CONSIDER]
1. Email OTP用テストユーザーの管理方針（realm-export固定 vs 初期化スクリプト）を `auth-stack/README.md` に明文化する。
2. 将来方式追加を見据え、`mfa_method` の許容値をドキュメント化する（`totp|email|...`）。

[GOOD]
- `mfa_enabled` → `mfa_method` 分岐の責務分離が明快で、設定ミス時の影響範囲が小さい。
- Required Action (`email-authenticator-setup`) まで含め、実運用フローを意識した実装になっている。
- Docker/Realm/運用ドキュメントを同時に更新しており、導入手順が実務的。